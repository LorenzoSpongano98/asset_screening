[project]
name = "asymmetric-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "polars>=1.43.2",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "pytest>=9.1.1",
    "pyyaml>=6.0.3",
    "yfinance>=1.5.2",
]
[tool.pytest.ini_options]
pythonpath = [
  "."
]
